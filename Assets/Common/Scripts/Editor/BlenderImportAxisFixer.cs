using UnityEditor;
using UnityEngine;
/*
public class BlenderImportAxisFixer : AssetPostprocessor {
    void OnPostprocessModel(GameObject g) {
        if (assetPath.EndsWith(".blend")) {
            g.transform.rotation = Quaternion.Euler(40, 30, 0); // Correct Z-up to Y-up
        }
    }
}
*/